[tool.coverage.run]
parallel = true
branch = true
source = ["{{cookiecutter.package_name}}"]

[tool.coverage.paths]
source = ["src", ".tox/*/site-packages"]

[tool.coverage.report]
show_missing = true
